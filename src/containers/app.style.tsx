import styled from "styled-components";
import { Button, Container } from "@mui/material";

export const CustomizedSlider = styled(Button)`
  color: white;
  background-color: #0277bd;
`;

export const DropContainer = styled(Container)`
  background-color: #b3e5fc;
  min-height: 500px;
  overflow: auto;
`;
